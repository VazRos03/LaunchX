import React from "react";
import Checkbox from './Checkbox';

const TaskList = props => {
    const [lis, setList] = props;

    const checkbox = list.map()
    return(
        <div>
            
        </div>
    );
}

export default TaskList;